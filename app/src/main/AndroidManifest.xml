<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.miles.fitness4u">


    <!--Creates a permisson for the app to use the internet-->
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <!-- Feature only available if the device is within current update-->
    <uses-feature android:name="android.hardware.Sensor.stepcounter"/>
    <uses-feature android:name="android.hardware.Sensor.stepdetector"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AccountActivity.UserLoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NavDrawerActivity"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".AccountActivity.UserSignupActivity" />
        <activity android:name=".AccountActivity.UserResetPasswordActivity"></activity>



    </application>

</manifest>